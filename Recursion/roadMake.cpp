#include <iostream>
#include <vector>
#include <unordered_map>
#include <map>
#include <queue>
#include <stack>
#include <algorithm>
#include <limits>
#include <list>
using namespace std;

class A {
};
vector<vector<int>>makeRoad(int wid,int len, int currLen,int currWidth)
{
    if(currWidth>=wid&&currLen>=len)
    {
        res.push_back(temp)
    }
}
int main() {
    
    return 0;
}